/**
 * Used to define a  center (lat, lon) and a zoom level for an example loaded
 * to a leaflet map
 *
 * @export
 * @interface MapViewDefinition
 */
export interface MapViewDefinition {
  center: Array<number>;
  zoom: number;
}
